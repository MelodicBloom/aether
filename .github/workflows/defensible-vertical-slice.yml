name: Defensible Vertical Slice
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    uses: MelodicBloom/.github/.github/workflows/reusable-defensible-vertical-slice.yml@214f41c65517d3158f50e99a54141566c45ee6a6
    with:
      node-version: "20"
      package-manager: npm
      profile-path: .melodicbloom/verification-profile.json
      lint-command: npm run lint
      typecheck-command: npm run typecheck
      build-command: npm run build
      specialized-command: test -s docs/verification/WEBGL_SMOKE_TEST.md && test -s docs/verification/ACCESSIBILITY_CHECKLIST.md
      artifact-path: .next
